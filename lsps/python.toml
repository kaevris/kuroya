id = "python"
display_name = "pyright"
languages = ["python"]

[install]
kind = "unsupported"
reason = "requires Node.js; install via npm: npm install -g pyright"
fallback_shell = "npm install -g pyright"

[verify]
command = "pyright-langserver"
args = ["--version"]
