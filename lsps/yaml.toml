id = "yaml"
display_name = "yaml-language-server"
languages = ["yaml"]
launch = "yaml-language-server"
install = "npm"
npm_fallback = "npm install -g yaml-language-server"
reason = "requires Node.js and npm"

[verify]
command = "yaml-language-server"
args = ["--version"]
