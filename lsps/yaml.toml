id = "yaml"
display_name = "yaml-language-server"
languages = ["yaml"]

[install]
kind = "unsupported"
reason = "requires Node.js; install via npm: npm install -g yaml-language-server"
fallback_shell = "npm install -g yaml-language-server"

[verify]
command = "yaml-language-server"
args = ["--version"]
